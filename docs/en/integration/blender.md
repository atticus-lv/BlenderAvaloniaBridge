# Blender Addon Integration

The Blender-addon-side integration steps are now part of the main [Integration Guide](./index.md).

If you want to see how the Avalonia app uses `BlenderApi` with the `Rna`, `Ops`, and `Observe` domains, go to the [API section](../api/index.md).
